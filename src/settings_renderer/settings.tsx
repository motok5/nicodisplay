import React from 'react';
import ReactDOM from 'react-dom';
import SettingsApp from './components/settings_app';

ReactDOM.render(<SettingsApp />, document.getElementById('contents')); //settings_appを読み込む

//最初に読み込み
